# Brief archive — 2026-08-06

Items retired from the Weekly Intel Brief on 2026-08-06, kept verbatim so nothing decided here is lost.
The brief itself is the *live* board and only carries open work; this file is the record.

**Live board:** https://claude.ai/code/artifact/fd19e15d-9757-4e0d-9433-b78348329075

IDs are stable and survive rewording, so `grep` for an `ix-*` id here when a decision needs tracing.

---

## Iurix (81 items)

### `ix-domain` · Max

Cloudflare domain cutover to iurixaccreditation.com — Worker custom domain live (verified serving HTTP 200)

### `ix-supabase` · Max

Supabase projects renamed to Iurix — both prod and staging (verified via API)

### `ix-certnum` · Terminal

Certificate number format CERT-YYYYMMDD-##### → IX-YYYYMMDD-#### (migration 0014)

### `ix-certsig` · Terminal

Certificate signature line — “Authorized by Built Smart by Rob” → “Reviewed and signed by” + blank line

### `ix-dpa` · Terminal

DPA processor entity — now reads “BSBR Holdings, LLC d/b/a Iurix”

### `ix-certpdf` · Terminal

Cert PDF rebuild — Stack Sans via fontkit, employee name + quiz score plumbed through, landscape monochrome layout, QR placeholder

### `ix-emailname` · Terminal

Cert delivery email greets the employee by name instead of their email address

### `ix-deploy` · Max

Deployed everything from 07-28 (app 13:12Z, then app + cert-worker redeployed 13:47Z/13:48Z)

### `ix-contactdom` · Max

Cert footer contact moved off the unowned iurix.com → accreditation@iurixaccreditation.com. ⚠ Creating the mailbox is now tracked as its own open item (ix-certmailbox) rather than a footnote in this done row

### `ix-certworker` · Terminal

cert-worker APP_URL repointed at iurixaccreditation.com + redeployed (first deploy since 06-24)

### `ix-e2e3` · Max

Supabase Auth URLs (Site URL + redirect allow-list) and the cert-generation webhook repointed at the new domain

### `ix-seatbug` · Terminal

Seat double-count fixed — migration 0015 makes the DB trigger the single source of truth (occupies_seat + status), the three manual used_seats increments are gone, and an admin only consumes a seat if they opt into training. 3 commits, pushed, deployed 16:31Z. Reconciled 14 firms, 0 mismatches

### `ix-resend` · Rob

Resend domain verified on iurixaccreditation.com by Rob — DKIM, SPF and DMARC all confirmed live. Ends the 403 outage that had killed onboarding, invites, cert delivery and reminders

### `ix-fromaddr` · Terminal

Transactional email now sends from IURIX &lt;noreply@iurixaccreditation.com&gt; (lib/resend.ts + the cert-worker's duplicate). noreply is deliberate — the zone has no inbound MX, so replies would bounce. Verified: a real invite arrived correctly branded

### `ix-bsbrfooter` · Terminal

“Built Smart by Rob” removed from the marketing pages (footer.tsx + features-section.tsx, 3 spots) — commit de04d35, deployed 15:23Z. Verified live: 0 occurrences on / and /pricing. Note the remaining “athena” strings on those pages are CSS class names only (athena-pill, athena-dotgrid) — invisible to users and deliberately left alone

### `ix-seatgate` · Terminal

Seat-occupancy gate shipped and deployed — lib/seats uses the same predicate as the sync_used_seats trigger, so access and billing derive from one rule. quiz/attempt returns 403 before any write; the three pages render a gate state. Commit ef31d84. STILL NEEDS the signed-in walkthrough to confirm end to end

### `ix-reassign` · Terminal

Reassignment now revokes the departing member's access (mirrors delete's app_metadata clear), records and logs preserved. Commit 4634512, deployed

### `ix-athena` · Terminal

“athena.” wordmark retired from login, forgot-password, update-password, onboarding and the site footer, plus two aria-labels; monogram kept. Verified live: 0 occurrences on /login. Commit 56f58dd

### `ix-emailcopy` · Terminal

employee-invite.tsx “short video course” copy corrected — the course is Articulate Rise interactive content. Shipped with the Task 5 commit

### `ix-silentcatch` · Terminal

Invite + bulk-invite now report emailSent:false instead of unqualified success; firm_members.invite_email_failed (migration 0016) persists it and the team table badges “Invite not delivered”. /api/invite/resend clears it. Commit e8c1864, deployed

### `ix-nextup` · Terminal

“Next Up” now resolves to the resume lesson, then the outstanding check, then the assessment. Avoided a trap: !contentViewed can structurally never be true (Rise reports passed-incomplete), which would have pinned it on “resume content” forever. Commit 8d799c6

### `ix-activityspam` · Terminal

Activity feed collapses repeated “Started the training content” at the DISPLAY layer only — the write path and the Rule 5.3 audit export keep every launch. Fetch limit raised to 40 so the feed gets richer, not shorter. Commit 9489d61

### `ix-seatshover` · Terminal

Warning is now a hover/focus/tap reveal with aria-expanded. COMPROMISE: disabled buttons aren't focusable, so “Out of seats!” stays visible as the trigger and the rest reveals. Zero permanent footprint would need a different trigger element. Commit 3d40ba8

### `ix-quizcontinue` · Terminal

Continue button reachable — the bottom tab bar now hides while any quiz is open, via the same <html>-class pattern focus mode uses. Fixed the stacking trap rather than fighting it with z-index. Commit 0637d7c

### `ix-quizfullscreen` · Terminal

Quiz fills the viewport, type hierarchy rebuilt, answers in a single column with real spacing. Nav hidden during a quiz so a mis-click can no longer abandon an attempt. Commits 0637d7c + dad01e6

### `ix-quizicon` · Terminal

The “?” icon is gone entirely (Max wanted no icon, not a new one) and QuestionIcon deleted. Question text now takes the full width. Commit fc83add

### `ix-lessoncleared` · Terminal

Lesson-cleared card rebuilt as a celebration reusing the path flag motif; degrades to a static planted flag under prefers-reduced-motion. Icon extracted to its own file to avoid an import cycle. Commit a8a03ba

### `ix-takeaways` · Terminal

Key-takeaway markers are filled dots, not empty rings. Commit d7a16d5

### `ix-lockicons` · Terminal

Padlocks re-centred — the cause was artwork, not layout: the paths span y=7..21 so their optical centre is y=14 while the 24×24 viewBox centres on y=12. Shifted the viewBox window. Commit 486d698

### `ix-avgalign` · Terminal

“Average” aligned to the far right of the readiness row. Commit 486d698

### `ix-yourpath` · Terminal

Path map scales across 1280–1920px; the locked certificate pill now opens the outstanding check (routing to Quizzes was a no-op — the pill lives on that page). Commit 486d698

### `ix-navscope` · Terminal

Bottom tab bar scoped to Training only. Orthogonal to the quiz-hiding CSS: the gate decides whether to render, the class hides what is rendered. Commit ba04fc3

### `ix-navorder` · Terminal

Top nav reordered to Training → Support → Settings. Commit ba04fc3

### `ix-certdenom` · Terminal

Denominator now counts only seat-holders, via hasTrainingAccess from lib/seats (9 call sites share the predicate). Also fixed the same inflation in the forecast and avatar stack, plus a remainingSeats floor that meant the forecast could never complete. Commit 441fb4e

### `ix-deploypending` · Max

✅ CLOSED 2026-07-31 14:04 MST — EVERYTHING PUSHED AND DEPLOYED. origin/main = 277056f, local in sync, tree clean, 24 commits for the session (⚠ CORRECTED 2026-08-03: this row said 23. Actual is 24, 5370fc3 through 277056f. The wrong number here is where the 23 in Monday's startup came from). Migration 0017 applied (verified local/remote/time). App deployed 21:01:39Z (version 0c4e7ff8); cert-worker 19:48:34Z — Batch 3 touches NO cert-worker code (git diff 61965d7..7f65a19 returns zero such paths), so it correctly needed no second deploy. LIVE-VERIFIED AFTER DEPLOY: /api/health ok · GET /api/billing/summary → 401 Unauthorized · POST /api/billing/auto-renew → 401 · GET /api/billing/auto-renew → 405 (only POST exported) · bogus sibling route → 404 · /cookies 200 · ZERO info@aistaffcompliance.com on /login · ZERO Built Smart by Rob on /. ⚠ ONE TRANSIENT WORTH KNOWING: immediately after deploying, GET /api/billing/summary returned 404 for ~1 minute while POST returned 405 — it settled to the correct 401. Do not read a fresh-deploy 404 as a broken route without retesting

### `ix-reassignui` · Terminal

Panel no longer scrolls or clips. The clipped name field was not the field — the parent is overflow-y-auto and CSS forces the other axis to auto too, clipping the focus ring. Commit b9237a4

### `ix-seatstext` · Terminal

Out-of-seats copy replaced, centred, and “Billing” now opens the Stripe portal via the existing /api/portal route. Commit 3d40ba8

### `ix-reminderword` · Terminal

Reminder reworded to “Your firm can't be certified until everyone completes their training.” Found a THIRD copy during the audit — the manual Remind button was sending a different subject, so the same reminder read differently depending on whether a human or the cron sent it. All three now match. Commits b7098e9 + 94cea02

### `ix-empname` · Terminal

Name captured at set-password, written atomically with the password via updateUser rather than a follow-up admin call (which could have left someone nameless if it failed). Settings already had a working field. Commit 07ed1ac

### `ix-remindercheck` · Terminal

Audit complete — .planning/REMINDER-SYSTEM-AUDIT.md. No conflict between the toggle and the cron, but four findings logged separately below. Commit ff9765b

### `ix-handoffwrong` · Max

⚠ FIXED 2026-07-31 14:04 MST — the committed handoff contained a claim that was WRONG when written. session_handoff.md and .planning/sessions/20260731-max-summary.md both stated that supabase db push had NOT run and that 0017 was unapplied, so the nudge audit row was silently failing. It HAD run; 0017 was already applied, verified against the remote. Two further rows were merely stale by minutes (Batch 3 unpushed, nothing deployed) — both true when written, both false within the hour. ALL THREE CORRECTED IN PLACE with a dated note, committed as 277056f and pushed, because these are the files ROB reads and an uncorrected version would have told him a working feature was broken. LESSON: a handoff written before the wrap actions complete will misdescribe them — write it last, or correct it after

### `ix-billingpage` · Terminal

✅ BUILT, PUSHED AND DEPLOYED — Batch 3, 5 commits (368dff4 → 612dc56), plan at ~/.claude/plans/iurix-b3-billing-page.md. Live as of the 21:01:39Z deploy. DELIVERED: GET /api/billing/summary (subscription + last 12 invoices), POST /api/billing/auto-renew, /dashboard/billing, a cancellation email, and the Settings section reduced to a single pointer row. Both routes verified live: 401 unauthenticated, 403 non-admin. No cancel_at_period_end column — read live from Stripe. apiVersion left at 2026-05-27.dahlia. ✅ HARD CONSTRAINT VERIFIED LIVE by terminal against sandbox acct_1ThDpr6ZCSojEKRr: cancel → status stayed ACTIVE, cancel_at_period_end true, period end UNMOVED at 2027-07-30; resume → back to false. The unmoved period end proves nothing paid-for was destroyed. Claude independently confirmed the only subscriptions.cancel( occurrence in the repo is a COMMENT warning against it. REMAINING (ix-cancelemail): Max reviews how the cancellation email LOOKS, and confirms the sandbox is restored

### `ix-remindaudit1` · Terminal

DONE — the manual nudge now writes a training_events row and is rate-limited to one per employee per 48h (commit 0ed337b). ✅ BUILT + PUSHED + LIVE (app deploy 2026-07-31 19:44:52Z). Migration 0017 confirmed applied, so the audit row actually writes. Event write lives in app/api/invite/remind/route.ts, deliberately NOT in the shared sendTrainingReminder helper, so /api/invite/resend (the bounced-invite recovery path) cannot log itself as a supervision nudge

### `ix-nudgerename` · Terminal

DONE — visible “Remind” copy renamed to “Nudge” (commit 406f797). ✅ BUILT + PUSHED + LIVE (app deploy 2026-07-31 19:44:52Z). API route path and email subject lines deliberately untouched. ix-icons should be designed WITH this name, per Max

### `ix-nudgevisible` · Terminal

DONE — separate nudge_sent event type, and the inactivity cron dedupes against BOTH types (commits 484e318 + 93499f7). Keeps the audit distinction between the system auto-sending and an attorney personally chasing someone. ✅ LIVE — pushed and deployed (app 2026-07-31 19:44:52Z, cert-worker 19:48:34Z). Migration 0017 is applied, so both halves are functional, not inert

### `ix-remindaudit2` · Terminal

DONE — renewal dedupe widened 24h → 8 days (commit 3ff1ae6), matching what the expiry job already used. ✅ LIVE — pushed and deployed (app 2026-07-31 19:44:52Z, cert-worker 19:48:34Z). WHY 8 AND NOT 1 HOUR (Max first proposed an hour): a bucket matches within ±1 day, so the memory must outlast the whole 3-DAY eligibility window, not just the gap between two runs. Unexercised in the wild — no firm is inside a renewal window until mid-2027

### `ix-autobilling` · Terminal

DONE, BOTH HALVES ✅ LIVE — pushed and deployed (app 2026-07-31 19:44:52Z, cert-worker 19:48:34Z). The pricing page now discloses auto-renewal BEFORE payment (commit 809b9d4) — there was previously NO pre-payment disclosure anywhere, only “flat on renewal” as a price fact. The renewal EMAIL now states the card on file will be charged (commit 55530b8, cert-worker). Deliberately states no computed dollar amount — volume bands, seat changes and tax make it unreliable, and a wrong figure on a billing notice is worse than none; it points at the Stripe portal for the exact amount. Feeds ix-stripeaudit

### `ix-autorenewsettings` · Terminal

✅ DONE AND LIVE (21:01:39Z deploy) — the two-buttons-one-destination problem Max flagged is fixed. Batch 2 shipped a Billing section whose rows BOTH deep-linked the same Stripe portal; Batch 3 reduced it to a single short row pointing at /dashboard/billing (commit 612dc56), using Max's own shorter copy. The #billing anchor and nav entry are preserved — load-bearing, because the pricing page's legal auto-renewal disclosure names “Settings → Billing” as the cancellation path

### `ix-remindaudit4` · Terminal

DONE — lapsed firms get 30/7 instead of 90/30/7, admin only (commit dbc858b). Never-certified already got nothing by construction, since the job queries the certificates table. ✅ LIVE — pushed and deployed (app 2026-07-31 19:44:52Z, cert-worker 19:48:34Z). ⚠ TERMINAL CORRECTED A FALSE PREMISE IN CLAUDE'S PLAN: the plan justified skipping the employee by claiming a lapsed firm's staff cannot sign in. They CAN — handleSubscriptionDeleted only sets status:cancelled and there is no firm-status gate in middleware or the dashboard layout. The decision stands on the real reason (they cannot re-certify while the firm is not paying) and that reason is now in the code

### `ix-skipcascade` · Terminal

✅ FIXED 2026-08-04 — commit a21aa59. Became a DEPENDENCY rather than just a bug: refund eligibility reads knowledge-check state to decide who gets money back, so a bug that fabricates cleared checks would have fabricated refund ineligibility. Fixed first, deliberately. WAS: BUG: clearing a later lesson check auto-marks an EARLIER skipped check as cleared. Same cascade the final-review shortcut uses, but firing between individual checks, which is wrong. Found in Max's run-through 2026-07-30

### `ix-quizwidth` · Terminal

APPROVED BY MAX — commit 4aedeaf. LOGGED 2026-07-31 11:25 MST (Max): “i like the choice from terminal and how it looks on quizzes.” The capped width (lg:max-w-5xl xl:max-w-6xl, ~1152px at 1920px) stands rather than going edge-to-edge, because full-sentence answers at ~1800px would be unreadable. Real cause was that all three bands each carried their own max-w-4xl. ✅ PUSHED AND DEPLOYED (app 2026-07-31 21:01:39Z)

### `ix-yourpathsize` · Terminal

APPROVED — commit 0601f68, covered by Max's “whatever i didnt mention is bc its ok” (2026-07-31 11:25 MST). Previous pass had both halves inverted: aspect-[4/7] means width is the only size input, so widening bought 1.75px of height per 1px (910px tall map); cap is now derived from viewport height, min(380px, 60svh*4/7). Clipping moved from viewport-keyed to @container-keyed because the box width is not monotonic in viewport width. Container CSS confirmed emitting. ✅ PUSHED AND DEPLOYED (app 2026-07-31 21:01:39Z)

### `ix-signinlinks` · Terminal

Sign-in footer fixed — commit 2cc9cc2, Batch 1 2026-07-31 10:53 MST. Terms now links to /terms (the page had existed all along; the “no page yet” comment was stale). Cookies span removed with a restore note pointing at ix-cookiespage. Support mailto repointed off the retired info@aistaffcompliance.com to solarsaiko@gmail.com, commented TEMPORARY pending Rob (C4). ✅ PUSHED AND DEPLOYED (app 2026-07-31 21:01:39Z)

### `ix-certverify` · Terminal

✅ BUILT 2026-08-04 — commit c2e7c07 + migration 0020, applied and verified. Public /verify route with QR, revocation, rate limiting and noindex. Keyed on a 128-bit random token, NOT the certificate number. ⚠ TERMINAL CORRECTED THE PLAN AND WAS RIGHT: I claimed certificate numbers were sequential; 0001 was, but 0014 made the tail RANDOM, so my “walk the counter” reasoning was wrong. The token survives for a sharper reason — a 4-digit tail is 10,000 guesses per date, and a bare existence check hands back exactly the issuance-volume leak 0014 was written to prevent. It also added holder_name/firm_name SNAPSHOTS I had missed: verification must agree with the printed artifact when a name changes, and it keeps auth.users off the public code path so the route structurally cannot leak an email. Anon probe confirms both new functions return 42501 and certificates reads empty with 12 rows present. ⚠ Stored PDFs are never re-rendered, so the 12 existing certificates keep the placeholder box. NOT DEPLOYED. WAS: 🔴 LAUNCH REQUIREMENT — CERTIFICATE VERIFICATION. Max 2026-08-04: “this HAS to be built or we dont ship.” Today lib/cert-pdf.ts:158 draws a 76pt placeholder box labelled QR with a comment saying there is no endpoint to point it at, and no /verify route exists. 🔴 THE DESIGN TRAP, caught before build: certificate numbers come from a sequential counter (generate_certificate_number), so keying the verify page on them lets anyone walk the sequence and harvest every holder’s name and employer — a client list and a staff directory in one scrape. THE FIX Max approved: a 128-bit random verification_token is the lookup key, the printed IX- number stays human-readable only, and a typed number is never a bare lookup. ALSO APPROVED: revocation columns added NOW (there is currently no way to invalidate a certificate issued in error or obtained by gaming), noindex on the route (or holders become searchable by name), per-IP rate limiting, and status showing valid/expired/REVOKED rather than merely ‘was issued’. Score is deliberately NOT shown — pass/fail and dates only. ⚠ Stored PDFs are never re-rendered, so existing certificates keep the placeholder box forever. Spec: Task 3 of ~/.claude/plans/iurix-prelaunch-batch.md

### `ix-refundeligibility` · Terminal

✅ BUILT 2026-08-04 — commit cf3a64e. Computes per-seat refund eligibility, scoped to the CURRENT SUBSCRIPTION TERM. A seat stops being refundable once that person has cleared four of the five knowledge checks, verifiably finished the courseware (contentViewed), or been issued a certificate. 🔴 NOT implemented as highestLesson >= 5, which was rejected on inspection: lesson 5 has a deliberate test-out shortcut so a learner can skip 1-4 entirely and read as >= 5 having cleared nothing, while someone who genuinely cleared 1-4 and never opened 5 reads as 4 and is exactly the person the rule must catch. Term-scoping is load-bearing — without it, year-one certificates would make every customer permanently unrefundable and Max's “window reopens on renewal” decision would be nullified. Depended on ix-skipcascade being fixed first, because a bug that fabricates cleared checks would fabricate refund INELIGIBILITY. ⚠ Computes eligibility only; issues no refunds. No refund API is called anywhere, by design. ✅ CLOSES POLICY-DECISIONS open item A: the threshold is four of five knowledge checks cleared, which genuinely is 80% of the checks, so the Terms clause can say that instead of the vaguer “consumed the training”. NOT DEPLOYED.

### `ix-retention` · Terminal

✅ BUILT 2026-08-04 — commit dd6281f, a purge job on the existing cert-worker daily cron. At 2 years, ip_address and user_agent are nulled. Individual quiz answers are nulled at 12 months, keeping score and passed. provisioning_failures rows are deleted 12 months after resolution. Certificates are never touched. ⚠ MY PLAN CONTRADICTED ITSELF AND TERMINAL CAUGHT IT: the retention table said BOTH “null them, keep the event row” AND “training_events rows → delete” at the same 2-year mark. It implemented only the unambiguous rules and left row deletion behind a disabled flag rather than guessing, reasoning that the mistakes are not symmetric — keeping rows too long is a one-line fix, deleting them wrongly destroys Rule 5.3 evidence behind certificates kept indefinitely. Correct. 🟢 MAX RESOLVED IT 2026-08-04: KEEP THE ROWS, STRIP THE IDENTIFIERS. The event row IS the evidence and it is what a certificate rests on. Terminal to remove the disabled purge path entirely rather than leave a flag someone can flip. CONSEQUENCE FOR THE PRIVACY POLICY: training activity is kept as long as the certificates it supports, i.e. indefinitely, and that must be stated rather than implied. NOT DEPLOYED.

### `ix-reconciliation` · Terminal

✅ BUILT 2026-08-04 — commit cca02a5, daily Stripe-vs-database check on the cert-worker cron. This is what backs the published promise that anyone charged who received nothing gets a full refund or access; provisioning_failures catches the three CLASSIFIED causes in seconds, but a silent failure (webhook never delivered, handler threw before writing) leaves no trace at all. Reconciliation is the only detector that does not need to know the failure mode in advance. Three directions: active Stripe subscription with no firm (customer harm), active firm with no live subscription (revenue leak), and seat-count drift. Silence when healthy. ⚠ TERMINAL FOUND A MIRROR IMAGE THE PLAN MISSED: the livemode guard was written for direction 1, but the DATABASE has no livemode column, so on a sandbox key direction 2 would report EVERY active firm as “access without payment” — a bigger false alarm than the one being prevented, arriving on the same first run. Directions 2 and 3 are now suppressed accordingly. BASELINE MEASURED 2026-08-03 against live data: 30 subscriptions vs 17 firms, 13 orphans in direction 1 (all sandbox test sessions), ZERO in directions 2 and 3 — genuine positive verification that subscription updates, deletions and the 0015 seat fix all work. ⚠ NEEDS TWO CERT-WORKER SECRETS BEFORE DEPLOY, both now set: STRIPE_SECRET_KEY and OPERATOR_ALERT_EMAIL. NOT DEPLOYED.

### `ix-usonly` · Terminal

✅ BUILT 2026-08-04 — nine commits across the batch, plus migration 0022 adding a non_us_billing reason. Katy's position via Max is a HARD NO on international data transfers: US law firms only, US sub-processors, no adequacy decisions, no standard contractual clauses. The Terms and DPA say so, and Max chose to ENFORCE it rather than publish a policy the product contradicts. ⚠ CONFIRMED: Stripe Checkout has NO billing-country allowlist — allowed_countries is shipping-only. So it is two layers: refuse to create the session for a non-US buyer, and validate customer_details.address.country in the webhook as a backstop, writing a non_us_billing row that reuses the existing provisioning_failures ledger, operator alert and honest /onboarding message rather than a parallel path. Charge-prevention, not refund — layer 1 exists so layer 2 rarely fires. 0022 verified applied and the new reason value accepted. NOT DEPLOYED.

### `ix-cisecrets` · Rob

✅ DONE 2026-08-05 (Rob). All five GitHub Actions secrets added; ZERO existed before, so runbook step 2 had never been done. This is what removes the dependency on one person’s Mac. Deploys now run: Actions → Build & deploy → Run workflow → target: production. TWO THINGS FOUND WHILE DOING IT, both kept because they will bite someone again. (1) The runbook’s step 2 table says to copy NEXT_PUBLIC_APP_URL from .env.local, which holds http://localhost:3000. That value is inlined into the BROWSER bundle at build time and forgot-form.tsx is a client component, so following the doc literally ships password-reset links pointing at localhost from a build that passes every check. The secret is set correctly to https://iurixaccreditation.com; the doc row is still wrong (see ix-cicleanup). (2) SIX earlier ‘successful’ CI runs had uploaded nothing. The step ran wrangler piped into tee, and a bash pipeline exits with tee’s status, so wrangler’s failure was masked and the job went green while printing ‘Preview uploaded’. Fixed with set -o pipefail. ⟨ORIGINAL ENTRY⟩ 🔴🔴 THE ONLY THING BLOCKING ROB FROM DEPLOYING AT ALL. Five GitHub repo secrets, added once in the browser: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID (4b2a402334decc9259d7317aaf9782f0), NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_APP_URL. Rob owns the repo so Max has no Settings access; Rob does it. ⚠ THE PREMISE IS REAL AND CLAUDE WOULD HAVE UNDER-RATED IT: OpenNext cannot build on native Windows — it bakes Windows path separators into the server manifests and workerd cannot resolve them, so the Worker returns 500 on EVERY route while looking like a clean build. Rob diagnosed it precisely and his fix (build on Linux in CI) requires rewriting nothing. ✅ THE PRODUCTION GATE IS GENUINE — verified in the workflow CODE, not the comment: preview runs on push, production requires workflow_dispatch with target=production. Pushing cannot deploy. ⚠ First run may fail once on a mistyped secret, a token missing Workers Scripts:Edit, or a YAML nit — a hiccup, not Windows returning. The three NEXT_PUBLIC_* are public by design (inlined into the browser bundle), so sharing them is not a credential leak.

### `ix-merge` · Terminal

✅ DONE 2026-08-05, BUT NOT THIS BRANCH. Rob merged the same two branches independently and pushed while merge-attempt was still under review, so two parallel merges existed and HIS is the one that landed. main and redesign-iurix now sit at the same commit and match production. .planning/MERGE-GUIDE.md is history: DO NOT REDO THE MERGE. merge-attempt is abandoned. Two content findings existed only in that branch’s handoff and would have been lost with it; they were rescued into .planning/sessions/20260805-max-summary.md §4 (see ix-privacyrise and ix-aitutor). 🔴 STILL TRUE FROM THE ENTRY BELOW: the merged tree carries ROB’S legal drafts, not ours. Building cleanly is not the v2 being settled. See ix-legalv2. ⟨ORIGINAL ENTRY⟩ 🟡 MERGE DONE ON A THROWAWAY BRANCH, AWAITING MAX'S REVIEW — commit 0a5ecbf on merge-attempt, off origin/redesign-iurix with origin/main merged in. 12 conflicts, all resolved. NOT pushed, NOT deployed. All six verification checks pass, confirmed independently: firstPerUser preserved and driving all three maps, zero avatar references in app/lib, the only raw brand hex is the token definitions in globals.css, cert contact is info@, migrations 0018-0023 each appear once, lib/avatars.ts deleted. tsc/eslint/build clean, 23 tests pass. ⚠ TERMINAL CAUGHT A TRAP NEITHER PLAN FLAGGED: dashboard/settings/page.tsx carries TWO kinds of change, avatars AND the hex-to-token conversion. Taking Rob's version — the intuitive move on a redesign branch — would have silently reverted raw hex into that file. ⚠ AND A BUG IN ITS OWN WORK: it staged pricing-slider.tsx, fixed a JSX problem, committed without re-staging, so the commit held the broken file while checks passed against the WORKING TREE. Caught it because the tree was not clean after, amended, then re-verified against a CLONE of the commit. 🔴 THE MERGED BRANCH CARRIES ROB'S LEGAL DRAFTS, NOT OURS. Building cleanly is NOT the v2 being settled — see ix-legalv2.

### `ix-avatarsgone` · Max

✅ THE CODE SIDE IS DONE 2026-08-05, landed in Rob’s merge and live in production. lib/avatars.ts deleted, upload route and component removed, rendering stripped from three dashboard files, zero references left outside migration history. 🔴 THE MIGRATION HALF IS NOT DONE AND IS NOW A DIFFERENT PROBLEM ENTIRELY: 0023 cannot run at all. Split out to ix-mig0023. The destructive-ORDERING warning below is now resolved (production no longer serves code that uses the bucket), so ordering is no longer the hazard; the hazard is that the migration is broken. ⟨ORIGINAL ENTRY⟩ 🟡 ROB DECIDED AVATARS ARE REMOVED ENTIRELY (MERGE-GUIDE decision 1, 2026-08-05), superseding our signed-URL work from the same morning. His call and defensible — deleting a feature beats securing it. Done in the merge: lib/avatars.ts deleted, upload route and component removed, rendering stripped from three dashboard files, zero references left. 🔴 THE ORDERING IS DESTRUCTIVE. 0023_remove_avatars.sql is `delete from storage.objects` then `delete from storage.buckets`. It is the ONLY unapplied migration. Rob's runbook lists it as a PRE-ship step; it is a POST-DEPLOY step. Production currently serves code that still uses that bucket, so applying it early breaks the live dashboard AND destroys the images with no rollback. Deploy the merge first, then push 0023. Migration 0019 (bucket → private) already ran and is harmless to leave. Terminal also found the migration stale in three ways, not the one Rob flagged: its header still said 0014 after two renames, and its cleanup cleared only avatar_url when our work added a second key, avatar_path.

### `ix-supportdest` · Rob

✅ ANSWERED 2026-08-04 — Rob confirmed info@iurixaccreditation.com. Shipped in commit c028370: app/api/support/contact/route.ts:13 now uses it, and the Stripe operator-alert fallback points there too. ⚠ THREE LIVE mailto: LINKS STILL POINT AT THE PERSONAL GMAIL, found by terminal after the sweep: app/login/page.tsx:72, app/onboarding/_components/onboarding-client.tsx:196, and SUPPORT_EMAIL in emails/checkout-email-in-use.tsx:43 — that last one is sent to someone we just charged and refused, telling them to write to a personal address. Queued for terminal. ⚠ Also unverified whether the info@ mailbox actually receives yet; the Worker alert secret is deliberately still the Gmail until that is confirmed. WAS: Question for Rob: where should support emails permanently go? app/api/support/contact/route.ts:8 currently hardcodes solarsaiko@gmail.com (already Max's inbox, as requested). Needs a real firm address once the contact details are decided

### `ix-gsdmissing` · Max

RESOLVED 2026-07-31 13:19 MST — NOT by editing CLAUDE.md. Max's call: “instead of doing that lets clear the ambiguity. write down a rule for my sessions. when i specify i am max. to disregard any gsd indications. i will just make sure to remind you each session.” Saved as a standing memory rule: when Max identifies himself, every GSD instruction in CLAUDE.md is disregarded, and Claude-written terminal prompts carry one terse line (/gsd:quick is not installed, proceed directly). No discussion needed each time. WHY NOT DELETE IT FROM THE REPO: CLAUDE.md is GSD-GENERATED — full of <!--GSD:section-start/end--> markers, and .planning/config.json sets claude_md_path to it. Deleting text without the markers lets a regeneration restore it, and the file is SHARED WITH ROB, whose machine may still have GSD installed. Scoping the fix to Max's sessions avoids breaking a shared file. .planning/ is unaffected and stays load-bearing — STATE.md, REQUIREMENTS.md, ROADMAP.md and sessions/ are all in active use. This was only ever about the command gate

### `ix-wholeft` · Terminal

Dead “View who's left” link removed — commit 22d4c50, Batch 1 2026-07-31 10:53 MST. It only scrollIntoView-ed a table already on screen. ✅ PUSHED AND DEPLOYED (app 2026-07-31 21:01:39Z)

### `ix-sweep` · Max

Rename sweep (RENAME-IURIX.md Layers 1–3) COMPLETE FOR STRINGS — verified 2026-07-31 against the source tree: zero “Built Smart by Rob”, zero “AI Staff Compliance”, zero workers.dev. Of the 26 remaining “athena” hits, 6 are cosmetic CSS class/keyframe names and 5 are comments, none user-visible. ⚠ The MARK is a separate matter and IS still shipping — see ix-monogram. Was carried as “roughly halfway” for days after the string work was actually finished

### `ix-appurl` · Max

NEXT_PUBLIC_APP_URL fixed and deployed. Root cause was that it lives in THREE places — Next inlines it at BUILD time from .env.local + .env.production, while wrangler.jsonc is runtime-only and never reaches the browser. That is why three deploys produced an identical chunk hash. Committed d593123

### `ix-emailfoot` · Max

Email footer links (privacy / terms / DPA) repointed at iurixaccreditation.com in the shared shell — all 5 templates. Committed d593123

### `ix-certmailbox` · Max

✅ CLOSED 2026-08-05 BY DELETING THE REQUIREMENT, NOT SATISFYING IT. Tracing it showed it was never a decision: c0718aa introduced accreditation@ on 07-28 as an EXPLICIT PLACEHOLDER (“no real contact address exists yet”) on iurix.com, a domain the project did not own; f3db0c3 fixed the domain and nobody revisited the local part. Used in exactly ONE place, lib/cert-pdf.ts. Meanwhile info@iurixaccreditation.com exists as a Zoho alias and is already on all three legal pages and the support form, and certificates now carry a QR to /verify so the main reason to contact us about one is self-service. Max's call: print info@, drop the second mailbox. Shipped in the merge. Carried as a PRE-LAUNCH BLOCKER for a week and was self-inflicted. WAS: 🔴 THE FIX CHANGED 2026-08-04 — DO NOT USE CLOUDFLARE EMAIL ROUTING. The apex DNS changed overnight: it now carries ZOHO MX (mx.zoho.com, mx2, mx3) and v=spf1 include:one.zoho.com, where on 08-03 it had zero MX and zero TXT (both states verified by dig). Configuring Cloudflare Email Routing now would OVERWRITE those MX records and break inbound mail. The mailbox should be created in Zoho instead. Whether accreditation@ actually exists yet is UNVERIFIED — MX records prove mail can be routed, not that a mailbox is provisioned. It is printed on the footer of every certificate. See ix-dnszoho. WAS: PRE-LAUNCH BLOCKER, was hiding inside a “done” row — the mailbox accreditation@iurixaccreditation.com DOES NOT EXIST, and it is printed on the footer of every certificate issued. Cert-worker item ix-contactdom moved the address off the unowned iurix.com and got marked done, which buried this. Needs Cloudflare Email Routing on the zone before a single real cert ships

### `ix-gentypes` · Terminal

CLOSED 2026-07-31 12:39 MST — the 07-30 hand-patch was CORRECT. Generated to a temp file against IURIX STAGING (ndmzvtuywcufvkxtkjhg, confirmed linked, PROD unlinked) and diffed: ZERO diff, byte-identical. Claude confirmed independently — types/supabase.ts has no commit since e8c1864 (the 0016 commit), temp file removed, tree clean. ⚠ TERMINAL'S CAVEAT, worth keeping: this result does NOT make hand-editing types safe. 0016 added ONE boolean to three blocks, about the easiest case there is. A column with a default, an enum, or a new relationship would be far likelier to diverge. The CLI is authenticated and linked on this machine, so generating properly is always available. 🔴 TRAP RECORDED SEPARATELY: training_events_event_type_check is restated in full by SIX migrations (0004, 0005, 0006, 0009, 0011, 0017); 0011 was current. Building 0017 from 0006 would have DELETED knowledge_check_completed and lesson_location_changed, both written by live routes, failing every knowledge-check submit. Postgres refused it. Always build from the LATEST definition

### `ix-lookupkey` · Terminal

✅ COMPLETED AND DEPLOYED 2026-08-06 in `dbb52ab`. Checkout now resolves the seat Price with lookup key `per_seat_annual`; the sandbox fallback is reachable only with a test key, while live mode fails closed if the key is missing. Twelve focused tests, typecheck, lint, build, a one-seat sandbox Checkout proof at $35/year, and GitHub Actions production run 31122263372 all passed. No card or purchase was entered.

⟨BOARD ENTRY RETIRED VERBATIM⟩ Branch stripe-lookup-key, built and preview-verified 08-05, NOT merged. main still carries the hardcoded sandbox price ID at checkout/route.ts:17. Merging removes a source edit and redeploy from the middle of the live-key cutover. Then create the live Price with lookup_key: per_seat_annual.

### `ix-refundnonus` · Terminal

✅ COMPLETED AND DEPLOYED 2026-08-06 in `c23192b`, included in the successful `dbb52ab` production deployment. The non-US cancellation email no longer promises a refund is under way; it says payment has not been returned and directs the buyer to `info@iurixaccreditation.com` to arrange it. The operator alert now correctly says that a human must issue any refund.

⟨BOARD ENTRY RETIRED VERBATIM⟩ 🔴 Live right now, and this is CODE, not an account, so it was never actually Rob’s. webhooks/stripe/route.ts:630 tells a non-US buyer in writing that their payment is being refunded. refunds.create appears ZERO times in the codebase, by design. Softening the wording is a normal edit we can make today; only the decision to actually issue refunds is Rob’s. Harmless on sandbox money, a written promise nobody keeps the day Stripe goes live. Blocks ix-stripeaudit.

### `ix-www522` · Max

✅ COMPLETED 2026-08-06. A permanent Cloudflare Redirect Rule now sends `http*://www.iurixaccreditation.com/*` to `https://iurixaccreditation.com/${2}` while preserving query strings. Browser proof: `https://www.iurixaccreditation.com/pricing?redirect-check=1` reached the apex pricing page with its query intact; the 522 is gone.

⟨BOARD ENTRY RETIRED VERBATIM⟩ 🔴 Max is Cloudflare admin, so this is his and it is one dashboard rule. www.iurixaccreditation.com returns 522 and always has. Re-confirmed by curl 08-06: apex 200, www 522. No x-opennext header, so www is not bound to the Worker and Cloudflare has no origin to reach. Needs a custom-domain entry or a redirect to the apex.

### `ix-mig0023` · Terminal

✅ Fixed and run 2026-08-06 (`3fe0ca4`). Migration is now a documented no-op; removal moved to `scripts/remove-avatars-bucket.mjs`, run once via the Storage API. Ran against staging: 1 object deleted, bucket dropped, confirmed by a direct `listBuckets()` call rather than trusting the script. One bug found in the same pass and fixed: `list()` on a missing bucket returns empty with no error in this SDK version, not the not-found error the script checked for, so it would have misreported bucket state on PROD. PROD never had this bucket, so nothing left to run there. Marked done on the board 2026-08-06 but not retired until now — an inconsistency caught while reviewing Codex's retirement of the other three Tier 0 items, fixed to match.

⟨BOARD ENTRY RETIRED VERBATIM⟩ 🔴 Armed trap. 0023_remove_avatars.sql cannot run: storage.protect_delete() now blocks SQL deletes from storage tables. It is DDL inside a transaction, so the next supabase db push fails AND rolls back whatever else was pending, on work unrelated to avatars. Rewrite it against the Storage API.

### `ix-devsandbox` · Max

✅ DONE 2026-08-06. Local dev now stays on STAGING permanently so the live site moving to PROD does not remove every safe place to click. .env.local keeps staging; a gitignored .env.prod holds PROD credentials and must be named explicitly (npx dotenv -e .env.prod -- ...). Test at localhost:3000 via pnpm dev, NOT on iurixaccreditation.com. ⚠ Reverses what PROD-CUTOVER.md originally said. Also fixed .gitignore, which listed .env.local by name and would have let .env.prod, holding the PROD service-role key, be committed. Full workflow in .planning/DEV-SANDBOX.md.

### `ix-supabasepro` · Max

✅ DONE 2026-08-05 (Rob). Upgraded to Supabase Pro and unpaused IURIX PROD (ttqthtzdjacrhjtrcmmy), which had been INACTIVE since the day it was created on 2026-06-11. ⚠ UNPAUSING DID NOT MAKE IT USABLE: it had zero migrations, zero tables, zero buckets. Everything that follows from that is now tracked separately at ix-prodcutover. ⟨ORIGINAL ENTRY⟩ PRE-LAUNCH — upgrade Supabase to Pro ($25/mo). The free tier PAUSES A PROJECT after 7 days idle and has no point-in-time recovery. Both are incompatible with paying customers, and a paused project means the whole product is down. Carried in STATE.md since Phase 0, never in this brief

### `ix-coursetitle` · Max

courses.title updated to “IURIX — Annual Certification” via SQL — verified, 1 row, nothing stale

### `ix-kcbug` · Terminal

Sequential gating dropped — lessons 1–4 are always available, so the Lesson 3 Q3/3 submit-time rejection is gone. Lesson 5's test-out keeps its contentViewed gate. Commit 5c1b76b

### `ix-worktree` · Max

Working tree wrapped — 4 commits pushed: domain fix, email sender, favicon swap (app/icon.png supersedes the scaffold favicon.ico), and the rename plan doc. main in sync, tree clean

### `ix-runbookfix` · Terminal

DOMAIN-CUTOVER.md corrected — §C1 now names .env.local and .env.production (build-time inlining) alongside wrangler.jsonc, and §E2 documents both webhooks incl. cert-worker-quiz-pass being permanently inert. Commit b30b2a6

### `ix-cwdeploy` · Max

Cert-worker redeployed 15:14Z — first deploy since 06-24. Closes the ambiguity carried from yesterday; it now definitely carries the noreply@ sender and the iurixaccreditation.com APP_URL

### `ix-orphanassets` · Terminal

Both orphaned assets deleted — commit 89082d7, Batch 1 2026-07-31 10:53 MST. Terminal also corrected FOUR rows in public/brand/README.md, not the two asked for: the cert-pdf placeholder row was stale (cert PDF is fixed) and the atc-logo.tsx row still described an “athena.” wordmark. Claude verified those corrections are accurate. ✅ PUSHED AND DEPLOYED (app 2026-07-31 21:01:39Z)

---
