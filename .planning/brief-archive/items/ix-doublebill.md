# `ix-doublebill`

**Owner:** Max · **State:** In progress · **Section:** Iurix

[Live board](https://claude.ai/code/artifact/fd19e15d-9757-4e0d-9433-b78348329075)

> Captured 2026-08-06, when this item had grown to **4,182 characters** on the
> board. The board now carries the current ask only. Everything below is the record: the
> reasoning, the verbatim decisions, and the corrections made to earlier claims.
>
> Append a new dated section rather than editing the ones below. A superseded claim that is
> still readable is worth more than a tidy file.

---

## Board text as of 2026-08-06

🟡 Deployed 08-05, but never exercised against a live webhook. Case 3 still leaves a subscription billing against nothing (Max ruled ‘cancel’ 08-03, unimplemented). The webhook safety net IS built; the remaining gap is that /api/checkout does no identity check, so the buyer is charged before anything stops them. Blocks ix-stripeaudit. Same problem as ix-winbackflow from the other end.

---

## Full text, captured 2026-08-06

🟡 STATUS CORRECTED 2026-08-06. IT IS DEPLOYED NOW: those 8 commits went to main, and main was merged and promoted on 2026-08-05, so the code is live. The rest of the entry below still stands and is why this is NOT done. Case 3 still leaves a subscription billing against nothing (Max ruled ‘cancel’ 2026-08-03 11:14 CST, never implemented), and ZERO of the four cases has ever been executed against a live webhook. ✅ ONE CORRECTION FROM ROB’S 08-05 AUDIT: the webhook SAFETY NET is genuinely built and BACKLOG.md #2 should be read as done. It resolves identity via find_user_id_by_email (0018), classifies duplicate / email_in_use / unresolved / non_us_billing, cancels the duplicate subscription and files a provisioning_failures row. What is still open is that the buyer is CHARGED AT ALL, because /api/checkout does no identity check up front (BACKLOG.md #1), plus the refund wording at ix-refundnonus. ⟨ORIGINAL ENTRY⟩ ✅ BUILT 2026-08-03 — 8 commits, pushed (57a8e59), NOT deployed, NOT yet tested end to end. Plan at ~/.claude/plans/iurix-doublebill.md. Identity resolution now runs BEFORE provisioning (owner beats member, active beats non-active): case 1 duplicate cancels the NEW subscription only, case 2 reactivates the returning client's existing firm with its certificates and audit trail intact, case 3 refuses and emails the buyer, case 4 provisions on the existing login. Migration 0018 applied (find_user_id_by_email + provisioning_failures ledger) and verified service-role-only by behavioural test. /onboarding no longer lies to a refused customer. ⚠ ONE OPEN ITEM: case 3 leaves a subscription billing against nothing — Max ruled “cancel” 2026-08-03 11:14 CST, not yet implemented. ⚠ ZERO of the four cases has ever been executed against a live webhook; everything is static verification only, so this CANNOT close ix-stripeaudit yet. ORIGINAL DIAGNOSIS BELOW, one part of which was WRONG: the brief said the agreed fix “cancels and refunds a legitimate paying customer”. It does not and never did — the 07-09 plan (260709-aeh, Rob, commits 52d0a98/52cf9f5) explicitly forbids auto-cancel and auto-refund. The real defect was that a returning customer was charged and provisioned nothing, with the only alert mailed to a RETIRED domain. — the 2026-07-06 fix DOES NOT HOLD UP. Stress-tested 2026-07-31 13:43 MST at Max's request. ✅ HALF OF IT IS ALREADY BUILT, which Claude had wrongly reported as missing: app/api/checkout/route.ts already checks for a logged-in user whose firm status is ACTIVE and returns { url: /api/portal }, routing them to billing instead of selling a second subscription. Claude claimed no pre-payment guard existed without ever opening that file. 🔴 THE REAL BUG IS NARROWER AND DIFFERENT. The guard fires only on status=active, so a CANCELLED firm's owner passes through to checkout deliberately — correct, a returning customer should be able to buy. Then handleCheckoutCompleted calls createUser, which fails because the email already has an auth account, and they are charged and provisioned NOTHING. The remaining bug IS the win-back customer (see ix-winbackflow), on the exact path ix-billingpage is designed to encourage. 🔴 THE 07-06 FIX GETS THE COMMON CASE BACKWARDS. It assumes two cases; there are THREE. (A) logged in + active firm → portal, ALREADY DONE. (B) email has an account but NO active firm — a returning customer, or an ex-employee starting their own practice → must be PROVISIONED, not refunded. (C) anonymous purchase on an email that HAS an active firm → genuine duplicate, cancel + refund is right. The agreed fix applies C's treatment to B, i.e. cancels and refunds a legitimate paying customer. ⚠ THREE THINGS UNSPECCED: the guard only protects users who happen to be LOGGED IN, and a signed-out customer is anonymous to it; cancelling a Stripe subscription does NOT refund the money (separate operation), so the buyer waits on a human, possibly across a weekend; and nothing specifies what the CUSTOMER sees — today they sit on /onboarding forever. MONDAY SCOPE: case B is the real work and is mostly “recognise a returning customer and give them their firm back” — a feature worth having, not damage control
