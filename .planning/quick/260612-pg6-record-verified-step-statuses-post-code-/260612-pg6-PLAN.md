---
phase: quick-260612-pg6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/NEXT-10-STEPS.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "NEXT-10-STEPS Steps 3,5,9 show VERIFIED status with the 2026-06-12 inspection evidence; Steps 4,6,7 show their corrected statuses"
    - "The leak/rotation/repo-migration incident is recorded as a one-line history note"
    - "The Verification Gaps section is resolved — prior four gaps marked closed, only Step 7 deploy confirmation left open"
    - "A Monday Smoke-Test Runbook (Step 10) section exists with a Max pre-flight list and a 7-check smoke sequence with per-check owners"
    - "STATE.md Supabase prerequisites row reflects confirmed ownership under Max's account + the new open question (prod project owner + Pro tier)"
  artifacts:
    - path: ".planning/NEXT-10-STEPS.md"
      provides: "Verified step statuses, resolved gaps, Monday runbook"
      contains: "Monday Smoke-Test Runbook"
    - path: ".planning/STATE.md"
      provides: "Updated Supabase prerequisites note"
      contains: "ndmzvtuywcufvkxtkjhg"
  key_links: []
---

<objective>
Update `.planning/NEXT-10-STEPS.md` to record the VERIFIED step statuses produced by Rob's 2026-06-12 code inspection (after Max's code was migrated to the correct repo `rtraversi/bsbr-attytraining`), resolve the now-stale Verification Gaps section, and add a Monday smoke-test runbook with a Max action list. Make one surgical edit to STATE.md's Supabase prerequisites row.

Purpose: The prior NEXT-10-STEPS statuses were "per Max's report — unverified" because no app code had been pushed. Code is now in the correct repo and inspected. This task converts hearsay statuses into verified statuses, closes the gaps, and gives both people a Monday runbook.

Output: Updated NEXT-10-STEPS.md (Steps 3–7, 9, Verification Gaps, new runbook section) + one updated STATE.md row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/NEXT-10-STEPS.md
@.planning/STATE.md

<facts>
Verified by inspection 2026-06-12 evening (treat as data):

INCIDENT (record as one-line history note in Step 4):
- Max's code first landed in the wrong repo (rtraversi/aistaffcompliance, the marketing site); its .open-next build output leaked the dev Supabase service-role key (project ndmzvtuywcufvkxtkjhg) in a public repo for ~90 min. CONTAINED: Max rotated the key, made the repo private, zero forks. No other secrets leaked (Stripe/Resend/Stream values were empty). Code then migrated to rtraversi/bsbr-attytraining as one clean squashed commit efc3214 (secret-free, co-authored Max Lugo); aistaffcompliance force-pushed back to marketing-only history (left private deliberately).

SUPABASE OWNERSHIP: dev project ndmzvtuywcufvkxtkjhg is under MAX's Supabase account (CONFIRMED). Open follow-up: decide whether the prod project should live under Rob's account + Pro tier before launch.

STEP STATUSES (replace the "per Max's report — unverified" annotations):
- Step 3 (OpenNext adapter): Done — VERIFIED 2026-06-12. wrangler.jsonc matches spec shape (main `.open-next/worker.js`, ASSETS binding, nodejs_compat, compatibility_date 2026-06-12, preview_urls true); open-next.config.ts present; package.json has preview/deploy/cf-typegen scripts; @opennextjs/cloudflare ^1.19.11 + wrangler ^4.99; NO `runtime = 'edge'` exports anywhere. Runtime check (pnpm run preview) happens in Monday smoke test.
- Step 4 (env vars): Done on Max's machine (contents unverifiable remotely). .gitignore verified to block .env*, .dev.vars, .open-next/, node_modules. Add the leak+rotation one-line history note.
- Step 5 (DB schema): Substantially Done — VERIFIED. migration 0001_initial_schema.sql (296 lines) creates 8 tables (firms, courses, seats, firm_members, enrollments, quiz_attempts, certificates, processed_stripe_events), RLS enabled on all 8, 12 indexes incl. firm_id indexes; types/supabase.ts (482 lines) generated against the live dev DB (proves `supabase db push` succeeded). GAP: missing `training_events` (audit log — COURSE-08 + AUDIT-01..03) and `cert_generation_queue` (cert dead-letter queue) → needs follow-up migration 0002 (Max).
- Step 6 (Supabase Auth wiring): NOT DONE / In Progress. middleware.ts, lib/supabase/client.ts, lib/supabase/server.ts are all 0-byte placeholder files. Deps installed (@supabase/ssr ^0.12.0, @supabase/supabase-js ^2.108.1). This is Max's TOP PRIORITY — the smoke test's auth check blocks on it.
- Step 7 (first deploy): Unverified — Cloudflare API not accessible from Rob's session, AND any prior deploy predated the repo fix, so Workers Builds may be connected to the wrong repo or not at all. Monday: Max provides the *.workers.dev URL + confirms Workers Builds is connected to bsbr-attytraining.
- Step 9 (cert Worker stub): Code Done — VERIFIED. workers/cert-worker/src/index.ts validates X-Webhook-Secret (401 on mismatch), 405 on non-POST, 400 on bad JSON, filters to quiz_attempts INSERT with passed=true, returns 500 for Supabase webhook retry; typed Env for 4 secrets; numbered TODO stubs for PDF/storage/email. Deploy + Supabase Database Webhook wiring still unverified.
- Minor notes: package.json "name" still "aistaffcompliance" (cosmetic rename to bsbr-attytraining suggested); React is 19.1.0 vs spec's 18.3 (create-next-app default; works with Next 15.5 — accept as a noted spec deviation, do not rewrite stack docs).

MAX LOCAL-CLONE RESET (CRITICAL — reset --hard, not pull; a plain pull reintroduces the dirty history):
  git remote set-url origin https://github.com/rtraversi/bsbr-attytraining.git && git fetch origin && git checkout main && git reset --hard origin/main && pnpm install
Rob is granting Max collaborator access to bsbr-attytraining.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Steps 3–7 and 9 statuses + record the incident</name>
  <files>.planning/NEXT-10-STEPS.md</files>
  <action>
Update the top banner line (line 4): keep the adapter-swap note and append that this edit (2026-06-12 evening) records VERIFIED statuses after code migration to rtraversi/bsbr-attytraining.

Rewrite each step's **Status** line and add evidence, using the verified facts in <facts>:

- Step 3 (line 42): change Status to `Done — VERIFIED by inspection 2026-06-12`. Add a short verified-evidence bullet/note under the header summarizing the wrangler.jsonc/open-next.config.ts/scripts/dependency checks and the no-edge-export confirmation; note the runtime `pnpm run preview` check is deferred to the Monday smoke test. Leave the existing instructional checkboxes intact (they remain a useful record); you may tick the boxes that the inspection confirmed are satisfied.
- Step 4 (line 82): change Status to `Done on Max's machine (file contents unverifiable remotely); .gitignore verified to block .env*, .dev.vars, .open-next/, node_modules`. Add a one-line history note recording the leak incident: dev Supabase service-role key (project ndmzvtuywcufvkxtkjhg) was exposed ~90 min in the public aistaffcompliance repo via leaked .open-next output; CONTAINED — key rotated by Max, repo made private, zero forks, no other secrets leaked; code migrated to bsbr-attytraining (clean squashed commit efc3214).
- Step 5 (line 105): change Status to `Substantially Done — VERIFIED 2026-06-12`. Replace the stale ownership parenthetical with the verified summary (0001_initial_schema.sql 296 lines, 8 tables, RLS on all 8, 12 indexes incl. firm_id; types/supabase.ts 482 lines generated against live dev DB). Add a GAP note: migration 0002 still needed for `training_events` (COURSE-08 + AUDIT-01..03) and `cert_generation_queue` (Max).
- Step 6 (line 125): change Status to `In Progress — files stubbed, no implementation (Max's TOP PRIORITY)`. Note that middleware.ts, lib/supabase/client.ts, lib/supabase/server.ts are 0-byte placeholders; @supabase/ssr ^0.12.0 + @supabase/supabase-js ^2.108.1 installed; the smoke test's auth check blocks on this.
- Step 7 (line 139): change Status to `Unverified — pending Monday confirmation`. Replace the stale "scaffold not pushed" reasoning with: code is now in bsbr-attytraining (efc3214); any prior deploy predated the repo fix so Workers Builds may be connected to the wrong repo or not at all; Monday Max provides the *.workers.dev URL + confirms Workers Builds is connected to bsbr-attytraining.
- Step 9 (line 181): change Status to `Code Done — VERIFIED 2026-06-12 (deploy + webhook wiring still unverified)`. Add the verified-evidence summary for workers/cert-worker/src/index.ts (X-Webhook-Secret 401, 405 non-POST, 400 bad JSON, filters quiz_attempts INSERT passed=true, 500 for webhook retry, typed Env for 4 secrets, numbered TODO stubs).

Add a short "Minor notes (2026-06-12)" line near the changed-statuses area or at the end of the affected steps: package.json name still "aistaffcompliance" (cosmetic rename suggested); React 19.1.0 vs spec's 18.3 — create-next-app default, works with Next 15.5, accepted as a noted spec deviation (do NOT rewrite stack docs).

Do NOT alter Steps 1, 2, 8, 10's content (Step 10 gets a NEW companion runbook section in Task 2, not a rewrite). Do NOT delete the existing instructional checkboxes — this file is Max's onboarding checklist.
  </action>
  <verify>
    <automated>cd /c/Sites/attytraining && grep -q "VERIFIED" .planning/NEXT-10-STEPS.md && grep -q "efc3214" .planning/NEXT-10-STEPS.md && grep -q "ndmzvtuywcufvkxtkjhg" .planning/NEXT-10-STEPS.md && grep -q "cert_generation_queue" .planning/NEXT-10-STEPS.md && echo OK</automated>
  </verify>
  <done>Steps 3,5,9 show VERIFIED with evidence; Steps 4,6,7 show corrected statuses; the leak/rotation/migration incident and minor notes are recorded; no other steps' content disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Verification Gaps + add Monday Smoke-Test Runbook</name>
  <files>.planning/NEXT-10-STEPS.md</files>
  <action>
Rewrite the "## Verification Gaps" section (lines 230–245). Replace the unverified narrative with a short "Resolved 2026-06-12" block stating all four prior gaps are now closed:
1. Code pushed/verifiable — closed (migrated to bsbr-attytraining, commit efc3214).
2. Supabase ownership — closed (dev project ndmzvtuywcufvkxtkjhg confirmed under Max's account).
3. Step 3→7 logical inconsistency — closed (Step 3 adapter work verified by inspection).
4. CF deploy — the ONE remaining open item (Step 7): confirm the *.workers.dev URL and that Workers Builds is connected to bsbr-attytraining (Monday).

Then ADD a new section "## Monday Smoke-Test Runbook (Step 10)" after the resolved gaps. Two subsections:

**Pre-flight (before the call) — Max unless noted:**
1. Max: reset local clone to the new repo — CRITICAL, use `git reset --hard origin/main`, NOT a plain `git pull` (a pull reintroduces the dirty history). Exact commands: `git remote set-url origin https://github.com/rtraversi/bsbr-attytraining.git && git fetch origin && git checkout main && git reset --hard origin/main && pnpm install`
2. Max: implement Step 6 auth wiring — client.ts `createBrowserClient`, server.ts `createServerClient` reading cookies via `cookies()` from `next/headers`, middleware.ts token refresh; use `getClaims()` not `getSession()`; no `@supabase/auth-helpers-nextjs`.
3. Max: migration 0002 adding `training_events` + `cert_generation_queue`; `supabase db push` + regenerate `types/supabase.ts`.
4. Max: update `.dev.vars` / `.env.local` with the ROTATED Supabase secret key.
5. Max: confirm Workers Builds is connected to rtraversi/bsbr-attytraining (not the old repo); redeploy.
6. Rob: grant Max collaborator access to bsbr-attytraining (in progress).

**Smoke-test sequence (run in order; owner per check):**
1. `pnpm dev` — pages load, no console errors (Max)
2. `pnpm run preview` — app serves in workerd with `.dev.vars` loaded (Max)
3. Supabase Auth test user; session survives navigation — middleware refresh confirmed (Max)
4. DB queries against the dev project visible in the Supabase table editor (Max)
5. Next.js Worker URL `*.workers.dev` responds (Max; Rob spot-checks)
6. cert Worker: `curl` with `X-Webhook-Secret` → 200; without → 401 (Rob can run the curl)
7. `stripe listen --forward-to localhost:3000/api/webhooks/stripe` forwards a test event (Rob's CLI + Max's dev server). NOTE: the webhook route doesn't exist yet (Phase 1 work) — for the smoke test the check is only that the Stripe CLI forwards and the dev server receives/404s, validating the pipe, not the handler.

Keep the existing Step 10 checklist (lines 191–203) as-is — this new runbook is the operational companion with owners + the Phase-1 caveat, not a replacement.
  </action>
  <verify>
    <automated>cd /c/Sites/attytraining && grep -q "Monday Smoke-Test Runbook" .planning/NEXT-10-STEPS.md && grep -q "Resolved 2026-06-12" .planning/NEXT-10-STEPS.md && grep -q "reset --hard" .planning/NEXT-10-STEPS.md && grep -q "validating the pipe, not the handler" .planning/NEXT-10-STEPS.md && echo OK</automated>
  </verify>
  <done>Verification Gaps section shows all four prior gaps resolved with only Step 7 deploy open; a Monday Smoke-Test Runbook section exists with a 6-item pre-flight list and a 7-check smoke sequence, each with an owner and the Stripe Phase-1 caveat noted.</done>
</task>

<task type="auto">
  <name>Task 3: Update the single STATE.md Supabase prerequisites row</name>
  <files>.planning/STATE.md</files>
  <action>
Edit ONLY the Supabase prerequisites table row (line 30). Replace the stale ownership warning with the confirmed status: ownership CONFIRMED under Max's Supabase account (dev project `ndmzvtuywcufvkxtkjhg`); open question shifts to whether the prod project should live under Rob's account + Pro tier before launch. Keep the row in the same table with the same Item/Owner columns; you may keep a ⚠️ marker for the still-open prod/Pro-tier question.

Do NOT touch any other part of STATE.md — specifically NOT the Quick Tasks Completed table (lines 110–119) and NOT the Session Continuity section (lines 127–140). Single-row edit only.
  </action>
  <verify>
    <automated>cd /c/Sites/attytraining && grep -q "ndmzvtuywcufvkxtkjhg" .planning/STATE.md && grep -q "CONFIRMED" .planning/STATE.md && echo OK</automated>
  </verify>
  <done>The Supabase prerequisites row reflects confirmed ownership under Max's account + the new prod/Pro-tier open question; no other STATE.md content changed.</done>
</task>

</tasks>

<verification>
- NEXT-10-STEPS.md: Steps 3,5,9 VERIFIED with evidence; Steps 4,6,7 corrected; incident + minor notes recorded; Gaps resolved; Monday runbook present.
- STATE.md: only the Supabase prerequisites row changed (Quick Tasks table + Session Continuity untouched).
- `git diff --stat` shows exactly two files modified.
</verification>

<success_criteria>
- All six step statuses replace the "per Max's report — unverified" annotations with verified/corrected statuses backed by the 2026-06-12 inspection evidence.
- The leak/rotation/repo-migration incident is captured as a concise history note.
- Verification Gaps section is resolved (four closed, one open: Step 7 deploy).
- Monday Smoke-Test Runbook (Step 10) section added with a Max pre-flight action list and a 7-check owner-assigned smoke sequence including the Stripe Phase-1 caveat.
- STATE.md Supabase row updated (single edit); Quick Tasks table and Session Continuity untouched.
</success_criteria>

<output>
Create `.planning/quick/260612-pg6-record-verified-step-statuses-post-code-/260612-pg6-SUMMARY.md` when done.
</output>
