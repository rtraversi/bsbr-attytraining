---
phase: quick
plan: 260709-b6w
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/health/route.ts
  - app/api/metrics/route.ts
  - load-tests/training-flow.js
  - load-tests/README.md
  - .planning/MONITORING.md
autonomous: true
requirements: [OBS-quick]
must_haves:
  truths:
    - "GET /api/health returns 200 when Supabase is reachable, 503 when it is not"
    - "GET /api/metrics returns 401 unless a valid x-metrics-secret header matches METRICS_SECRET"
    - "Authorized GET /api/metrics returns a distinct active-user count over the last 5 minutes"
    - "A dormant k6 skeleton and an operator-facing monitoring doc exist for future capacity work"
  artifacts:
    - path: "app/api/health/route.ts"
      provides: "Deep health check verifying Supabase connectivity"
      contains: "createAdminClient"
    - path: "app/api/metrics/route.ts"
      provides: "Secret-gated concurrency metric endpoint"
      contains: "METRICS_SECRET"
    - path: "load-tests/training-flow.js"
      provides: "Dormant k6 load-test skeleton"
    - path: "load-tests/README.md"
      provides: "How/when to run the dormant capacity test"
    - path: ".planning/MONITORING.md"
      provides: "Operator-facing monitoring + capacity-alerting roadmap"
  key_links:
    - from: "app/api/health/route.ts"
      to: "lib/supabase/admin.ts"
      via: "createAdminClient()"
      pattern: "createAdminClient"
    - from: "app/api/metrics/route.ts"
      to: "training_events"
      via: "distinct firm_member_id over event_timestamp window"
      pattern: "training_events"
---

<objective>
Add observability groundwork so the operator can detect capacity/concurrency pressure before it causes an outage — WITHOUT any schema change, migration, or type regeneration.

This is PURE ADDITIVE work: two new/upgraded API endpoints, a dormant k6 skeleton, and an operator-facing monitoring doc.

Purpose: Give BetterStack (and the future iurisdesk central hub) real signals — a deep health check and a live active-session count — plus a written capacity-alerting plan, ahead of the 2026-07-20 launch.
Output: `app/api/health/route.ts` (upgraded), `app/api/metrics/route.ts` (new), `load-tests/` (new), `.planning/MONITORING.md` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

From lib/supabase/admin.ts:
```typescript
// Service-role client — bypasses RLS. Server-side only.
export function createAdminClient()  // returns SupabaseClient<Database>
```
Usage pattern (from app/api/webhooks/stripe/route.ts):
```typescript
import { createAdminClient } from '@/lib/supabase/admin'
import { NextRequest, NextResponse } from 'next/server'
const supabase = createAdminClient()
```

CONFIRMED `training_events` columns (from supabase/migrations/0002_audit_and_queue.sql):
- id uuid
- firm_id uuid (not null)
- firm_member_id uuid (not null)   ← there is NO user_id column; firm_member_id is the per-person key
- event_type text
- ip_address text
- user_agent text
- metadata jsonb
- event_timestamp timestamptz not null default now()   ← the time column is event_timestamp, NOT created_at

CONFIRMED `courses` table exists and has an `id` column (used elsewhere as `.from('courses').select('id')`).

supabase-js has NO COUNT(DISTINCT) primitive. Chosen approach for the metric: select just the `firm_member_id` column for rows inside the window, then reduce to a distinct count in JS via a Set. Pre-launch volume is trivially small, so this is correct and cheap. Document this choice inline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep health endpoint verifying Supabase connectivity</name>
  <files>app/api/health/route.ts</files>
  <action>
Replace the static stub with a real connectivity check. Keep `export const runtime = 'nodejs'` (never edge). Endpoint stays public — no secret, no auth.

In GET(): import `createAdminClient` from '@/lib/supabase/admin'. Inside a try/catch, run ONE cheap head/count read against a stable table — use `await createAdminClient().from('courses').select('id', { head: true, count: 'exact' })`. Treat a returned `error` object the same as a thrown exception (both mean DB unreachable/failing).

On success: return `Response.json({ status: 'ok', db: 'ok', timestamp: new Date().toISOString() })` (HTTP 200 default).
On failure or throw: `console.error('[health] db check failed', err)` server-side ONLY, then return `Response.json({ status: 'degraded', db: 'error', timestamp: new Date().toISOString() }, { status: 503 })`.

Do NOT put the raw error, stack, or DB message in the response body — the body is public. Keep using `Response.json` (matches the current file's style).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/api/health/route.ts</automated>
  </verify>
  <done>File exports `runtime = 'nodejs'` and a GET that returns 200 {status:'ok',db:'ok',timestamp} on a successful courses head/count read and 503 {status:'degraded',db:'error',timestamp} on error/throw, with no raw error text in the body. tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Secret-gated concurrency metrics endpoint</name>
  <files>app/api/metrics/route.ts</files>
  <action>
Create a NEW route handler. Add `export const runtime = 'nodejs'` (never edge). Import `NextRequest, NextResponse` from 'next/server' and `createAdminClient` from '@/lib/supabase/admin'.

FAIL-CLOSED SECRET GATE (do this BEFORE any query): read `const provided = req.headers.get('x-metrics-secret')` and `const expected = process.env.METRICS_SECRET`. If `!expected || !provided || provided !== expected` → return `NextResponse.json({ error: 'Unauthorized' }, { status: 401 })` and run NO query. Never log the secret value (neither `provided` nor `expected`).

AUTHORIZED PATH: compute the 5-minute window: `const since = new Date(Date.now() - 5 * 60 * 1000).toISOString()`. Via `createAdminClient()`, select the per-person key for rows in the window:
`.from('training_events').select('firm_member_id').gte('event_timestamp', since)`.
Reduce to distinct with `new Set(rows.map(r => r.firm_member_id)).size` → `activeSessions`. Add an inline comment: training_events has no user_id column, so distinct firm_member_id proxies distinct active users; supabase-js has no COUNT(DISTINCT), and pre-launch volume is small, so a JS Set reduce over the windowed rows is the simplest correct approach. If the query returns an `error`, `console.error` it and return `NextResponse.json({ error: 'Query failed' }, { status: 500 })`.

Return `NextResponse.json({ timestamp: new Date().toISOString(), windowMinutes: 5, activeSessions })` (HTTP 200). Keep total queries at MOST 2 — a second `activeSessions15m` query is OPTIONAL; if you skip it, do not add it (don't invent extra fields). Add a top-of-file comment: "Consumed by BetterStack uptime/telemetry and the future iurisdesk.com central hub — keep the response shape stable."
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/api/metrics/route.ts</automated>
  </verify>
  <done>GET returns 401 with no query when METRICS_SECRET is unset OR the x-metrics-secret header is missing/mismatched; on a valid secret it returns 200 {timestamp, windowMinutes:5, activeSessions} where activeSessions is a distinct firm_member_id count over event_timestamp >= now-5min. runtime is nodejs. Secret is never logged. tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Dormant k6 training-flow skeleton</name>
  <files>load-tests/training-flow.js, load-tests/README.md</files>
  <action>
Create `load-tests/training-flow.js` as a COMMENTED k6 skeleton (plain JS — NOT TypeScript, excluded from tsc/eslint). Structure:
- `import http from 'k6/http'` and `import { sleep } from 'k6'`.
- `const BASE_URL = __ENV.BASE_URL || 'https://staging.example.invalid' // TODO: set to STAGING url via env`.
- `export const options = { stages: [ ... ] }` with a ramped VU profile (e.g. ramp-up to 10 VUs, hold, ramp to 25, hold, ramp-down) — values are placeholders.
- `export default function () { ... }` sketching the employee path as commented steps with TODOs: (1) login / obtain session — TODO wire real auth (Supabase session cookie), (2) read dashboard/progress — TODO real endpoint, (3) submit knowledge-check/quiz — TODO real endpoint + payload. Use `sleep(1)` between steps.
- Add a top banner comment: "DORMANT SKELETON — NOT wired to real endpoints, NOT for CI, NOT for pre-launch. Fill in TODOs before use. Target STAGING only, never production."

Create `load-tests/README.md`: explain this is a dormant capacity test; how to run (`k6 run -e BASE_URL=https://<staging> load-tests/training-flow.js`); that auth/session + real endpoints are unfilled TODOs; review target ~2026-08-19 (~30 days after the 2026-07-20 launch); and a bold warning to point at STAGING, never production.

Both files are .js/.md — exclude from tsc and eslint (no TS type-checking needed).
  </action>
  <verify>
    <automated>test -f load-tests/training-flow.js && test -f load-tests/README.md && grep -q "DORMANT" load-tests/training-flow.js && grep -q "2026-08-19" load-tests/README.md && echo OK</automated>
  </verify>
  <done>load-tests/training-flow.js is a commented k6 skeleton with staged VU options, BASE_URL from __ENV, TODO markers for auth + real endpoints, and a DORMANT/STAGING-only banner. load-tests/README.md documents how/when to run and the ~2026-08-19 review. Not referenced by app code, not in CI.</done>
</task>

<task type="auto">
  <name>Task 4: Operator-facing monitoring + capacity-alerting doc</name>
  <files>.planning/MONITORING.md</files>
  <action>
Create `.planning/MONITORING.md` (repo has no docs/ folder — operator docs live in .planning/, matching MARKETING.md / DEPLOY-CHECKLIST.md). DOCS ONLY — no code. Concise, skimmable, operator-facing (short sections, bullets, action items). Cover:

1. PRIMARY crash-prevention signal — alert on Supabase resource utilization (CPU / memory / connection-pool %) at ~70% sustained. Supabase Pro exposes a Prometheus-compatible metrics endpoint (service_role auth) that can be scraped into the existing BetterStack (Telemetry) for threshold alerts. Remediation when it fires = upgrade the Supabase compute add-on from the dashboard (brief restart). Frame `/api/metrics` `activeSessions` as the CORRELATING signal — it explains WHY utilization rose.
2. Uptime — BetterStack uptime monitor points at `/api/health` (the deep check upgraded in Task 1: 200 healthy / 503 degraded).
3. `/api/metrics` operational note — requires `METRICS_SECRET` set as a Worker secret (`wrangler secret put METRICS_SECRET`); fail-closed (returns 401) until set. ACTION ITEM: set the secret before relying on the metric.
4. iurisdesk.com central hub — a cross-brand aggregation dashboard that pulls `/api/metrics` from each property. Mark clearly as DESIGN SPIKE (its own planning session, not a quick task).
5. GitHub bug/feature submission — mirror the operator's other sites' pattern. Mark BLOCKED pending an example site to replicate.
6. k6 load test — dormant skeleton in `load-tests/`; review ~2026-08-19.

Keep it tight — this is a reference/roadmap, not prose.
  </action>
  <verify>
    <automated>test -f .planning/MONITORING.md && grep -q "METRICS_SECRET" .planning/MONITORING.md && grep -q "iurisdesk" .planning/MONITORING.md && echo OK</automated>
  </verify>
  <done>.planning/MONITORING.md exists covering: Supabase utilization ~70% as primary alert (BetterStack scrape of Supabase Prometheus endpoint, compute-add-on remediation), /api/metrics as correlating signal, /api/health uptime monitor, METRICS_SECRET wrangler action item + fail-closed note, iurisdesk hub as DESIGN SPIKE, GitHub submission BLOCKED pending example, k6 review ~2026-08-19.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` is clean across the repo after Tasks 1 & 2.
- `npx eslint app/api/health/route.ts app/api/metrics/route.ts` is clean.
- No migration file added, no `types/supabase.ts` change, no schema change.
- The metrics query references the REAL columns `firm_member_id` and `event_timestamp` (not user_id / created_at).
- Do NOT touch app/dashboard/layout.tsx, app/dashboard/_components/account-menu.tsx, or app/dashboard/quizzes/_components/quizzes-client.tsx.
- Do NOT stage .agents/, .claude/, or skills-lock.json in any commit.
</verification>

<success_criteria>
- /api/health does a real Supabase read and reports ok(200)/degraded(503) without leaking error detail.
- /api/metrics is fail-closed on METRICS_SECRET and returns a distinct 5-minute active-user count.
- load-tests/ holds a clearly dormant, staging-only k6 skeleton + README.
- .planning/MONITORING.md gives the operator a concrete capacity-alerting plan and roadmap.
- Four atomic commits (no schema/type churn):
  1. `feat(health): deep health check verifying Supabase connectivity`
  2. `feat(metrics): add secret-protected concurrency metrics endpoint`
  3. `chore(load-tests): add dormant k6 training-flow skeleton`
  4. `docs(monitoring): capacity alerting + observability roadmap`
</success_criteria>

<output>
Create `.planning/quick/260709-b6w-observability-groundwork-deep-health-che/260709-b6w-SUMMARY.md` when done.
</output>
