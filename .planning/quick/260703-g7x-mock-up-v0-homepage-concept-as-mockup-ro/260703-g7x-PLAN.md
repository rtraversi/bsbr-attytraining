---
phase: quick-260703-g7x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/mockup/page.tsx
  - app/mockup/_components/reveal.tsx
  - app/mockup/_components/site-header.tsx
  - app/mockup/_components/hero.tsx
  - app/mockup/_components/trust-bar.tsx
  - app/mockup/_components/features.tsx
  - app/mockup/_components/why-section.tsx
  - app/mockup/_components/site-footer.tsx
  - app/mockup/_components/pricing.tsx
autonomous: true
requirements: [QUICK-260703-g7x]
must_haves:
  truths:
    - "Visiting /mockup renders Rob's v0 homepage concept in a forced light palette"
    - "The existing Athena homepage at / and all app/_components + components/ui files are untouched"
    - "The pricing card shows the real volume bands ($35 / $32 / $28) with a live per-seat rate + total that updates as seats change"
    - "Clicking Enroll POSTs { seats } to /api/checkout and redirects to the returned Stripe url"
    - "npx tsc --noEmit and npm run lint both pass"
  artifacts:
    - path: "app/mockup/page.tsx"
      provides: "Light-palette wrapper + section assembly + scoped .reveal CSS + route metadata"
      contains: "export const metadata"
    - path: "app/mockup/_components/pricing.tsx"
      provides: "Volume-band pricing wired to /api/checkout"
      contains: "perSeatRate"
    - path: "app/mockup/_components/reveal.tsx"
      provides: "IntersectionObserver reveal util (self-contained copy)"
  key_links:
    - from: "app/mockup/_components/pricing.tsx"
      to: "/api/checkout"
      via: "fetch POST { seats }"
      pattern: "fetch\\([\"']/api/checkout"
    - from: "app/mockup/page.tsx"
      to: "app/mockup/_components/*"
      via: "imports and renders each section"
      pattern: "@/app/mockup/_components"
---

<objective>
Mock up Rob's v0 homepage concept inside the current site as a new, fully self-contained route at `/mockup`, using a forced LIGHT palette (no dark-mode media query). All v0 sections are ported into `app/mockup/_components/*`; pricing is wired to the real volume bands and the existing `/api/checkout`. The existing Athena homepage at `/` and all of Max's components are NOT touched.

Purpose: Give Rob a live, reviewable light-concept homepage without disturbing production work.
Output: `app/mockup/page.tsx` + 8 self-contained components under `app/mockup/_components/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<source_reference>
v0 export lives at `C:\Users\rtrav\Downloads\ai-training-website`. Port these files (adapt, do not symlink):
- `components/site-header.tsx`, `components/hero.tsx`, `components/trust-bar.tsx`,
  `components/features.tsx`, `components/why-section.tsx`, `components/site-footer.tsx`,
  `components/reveal.tsx`, `components/pricing.tsx`
Keep all v0 copy/wording verbatim (Rob revises later) EXCEPT pricing numbers (real bands) and stepper max (500).
</source_reference>

<interfaces>
<!-- Contracts the executor needs. Use these directly — no codebase exploration. -->

Existing Button (`components/ui/button.tsx`) — uses radix Slot via `asChild`, NOT a `render` prop:
```typescript
function Button(props: React.ComponentProps<"button"> & VariantProps<typeof buttonVariants> & { asChild?: boolean })
// asChild=true renders <Slot.Root> — wrap the anchor as a child.
```
CONVERSION RULE: every v0 `<Button render={<a href="…">…</a>} />` becomes
`<Button asChild><a href="…">…</a></Button>`.
There are exactly 3 `render=` usages to convert: 2 in `hero.tsx`, 1 in `site-header.tsx`.

`cn` util is at `@/lib/utils`. lucide-react is installed (^1.18). Geist sans is the default `--font-sans` — add no fonts.

Existing checkout route (`app/api/checkout/route.ts`) — DO NOT MODIFY:
```typescript
// POST /api/checkout  accepts { seats: number } (clamped 1–500 server-side)
// returns { url: string }  on success, or { error: string } on failure
```

v0 `.reveal` CSS (must be inlined in the mockup, NOT added to app/globals.css):
```css
.reveal { opacity: 0; transform: translateY(16px); transition: opacity .6s ease, transform .6s ease; }
.reveal.is-visible { opacity: 1; transform: translateY(0); }
@media (prefers-reduced-motion: reduce) { .reveal { opacity: 1; transform: none; transition: none; } }
```

v0 LIGHT palette CSS custom properties (apply as an inline `style` object on the /mockup wrapper div):
```
--background: oklch(0.985 0.003 250); --foreground: oklch(0.24 0.03 265);
--card: oklch(1 0 0); --card-foreground: oklch(0.24 0.03 265);
--primary: oklch(0.55 0.2 258); --primary-foreground: oklch(0.99 0.005 250);
--secondary: oklch(0.95 0.01 255); --secondary-foreground: oklch(0.3 0.04 265);
--muted: oklch(0.955 0.008 255); --muted-foreground: oklch(0.52 0.025 262);
--accent: oklch(0.93 0.03 255); --accent-foreground: oklch(0.3 0.05 262);
--border: oklch(0.9 0.012 258); --input: oklch(0.9 0.012 258); --ring: oklch(0.55 0.2 258);
color-scheme: light;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold /mockup route with light palette, scoped reveal CSS, and the Reveal util</name>
  <files>app/mockup/page.tsx, app/mockup/_components/reveal.tsx</files>
  <action>
Create `app/mockup/_components/reveal.tsx` as a verbatim copy of the v0 `components/reveal.tsx` (client component, IntersectionObserver, `.reveal`/`.is-visible` class toggling, `as`/`delay` props, imports `cn` from `@/lib/utils`). No behavior changes.

Create `app/mockup/page.tsx` (Server Component). It must:
- `export const metadata = { title: "Mockup — light concept" }`.
- Render a single wrapper `<div>` that carries the FULL v0 light palette as an inline `style` object of CSS custom properties (all `--background`…`--ring` values plus `colorScheme: "light"` from the interfaces block). Because these vars cascade, every Tailwind color utility (`bg-background`, `text-foreground`, `text-primary`, `border-border`, etc.) inside the subtree resolves to the light palette regardless of the system/root theme. Add classes `bg-background text-foreground flex min-h-screen flex-col` to the wrapper.
- Include a plain global `<style>` element (inside the wrapper) containing the three `.reveal` rules from the interfaces block, so the mockup is fully self-contained and `app/globals.css` is NOT edited. Do NOT add a dark-mode media query.
- Assemble the sections in v0 order inside the wrapper: SiteHeader, then `<main className="flex-1">` containing Hero, TrustBar, Features, WhySection, Pricing, then SiteFooter — importing each from `@/app/mockup/_components/*` (these are created in Tasks 2–3; imports may show as unresolved until then).

Do NOT touch `app/page.tsx`, `app/layout.tsx`, `app/globals.css`, `app/_components/*`, or `components/ui/*`.
  </action>
  <verify>
    <automated>test -f app/mockup/page.tsx && test -f app/mockup/_components/reveal.tsx && grep -q "metadata" app/mockup/page.tsx && grep -q "colorScheme" app/mockup/page.tsx && grep -q "prefers-reduced-motion" app/mockup/page.tsx</automated>
  </verify>
  <done>page.tsx exists with route metadata, an inline light-palette wrapper (CSS vars + colorScheme + bg-background/text-foreground), a scoped `<style>` block defining `.reveal`, and section imports in v0 order; reveal.tsx is a working self-contained copy. app/globals.css unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Port the six presentational sections, converting render props to asChild</name>
  <files>app/mockup/_components/site-header.tsx, app/mockup/_components/hero.tsx, app/mockup/_components/trust-bar.tsx, app/mockup/_components/features.tsx, app/mockup/_components/why-section.tsx, app/mockup/_components/site-footer.tsx</files>
  <action>
Copy each of the six v0 components into `app/mockup/_components/` with these adaptations only:
- Rewrite every import of `@/components/reveal` to `@/app/mockup/_components/reveal`. Keep `@/components/ui/button`, `@/lib/utils`, and `lucide-react` imports as-is (those are shared, read-only, and correct).
- Convert the 3 `render={<a …/>}` Button usages to the `asChild` pattern: `<Button …props><a href="…">…children…</a></Button>` (2 in hero.tsx, 1 in site-header.tsx). Preserve all className/size/variant props and the anchor children (including the `ArrowRight` icon in hero's first CTA). See the CONVERSION RULE in `<interfaces>`.
- Keep all other markup, copy, lucide icon choices (Lock, GraduationCap, ScanSearch, Eye, FileCheck2, Users; Clock, ShieldCheck; Minus/Plus etc.), animation classes, and structure verbatim — this is a visual port, not a redesign.

Do NOT edit any file outside `app/mockup/_components/`.
  </action>
  <verify>
    <automated>for f in site-header hero trust-bar features why-section site-footer; do test -f app/mockup/_components/$f.tsx || { echo "missing $f"; exit 1; }; done; ! grep -rn "render=" app/mockup/_components/ && ! grep -rn "@/components/reveal" app/mockup/_components/</automated>
  </verify>
  <done>All six sections exist under app/mockup/_components/, contain zero `render=` Button props (all converted to asChild), and reference the local `_components/reveal` (no `@/components/reveal`). Wording preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Port pricing with real volume bands wired to /api/checkout, then typecheck + lint</name>
  <files>app/mockup/_components/pricing.tsx</files>
  <action>
Copy v0 `components/pricing.tsx` into `app/mockup/_components/pricing.tsx` and adapt:
- Rewrite the `@/components/reveal` import to `@/app/mockup/_components/reveal`. Keep Button, lucide imports as-is.
- REPLACE the flat `const PRICE_PER_SEAT = 49` with a real volume-band function `perSeatRate(seats: number): number` implementing the LOCKED bands: 1–9 seats → 35, 10–24 → 32, 25+ → 28 (per STATE.md pricing decision, 2026-06-12). Compute `const rate = perSeatRate(seats)` and `const total = seats * rate`.
- Update the displayed per-seat price to show the live `rate` (e.g. `${rate}` in the "Per seat" header) so it changes as the band changes, and keep the live `${total.toLocaleString()}` total.
- Cap the stepper at 500 (not 1000): change the `updateSeats` clamp to `Math.min(500, next)`, the `<input max>` to 500, and the increment button `disabled={seats >= 500}`. Keep min 1 and the Minus/Plus stepper UI.
- Keep the checkout handler exactly matching the verified contract: POST `/api/checkout` with `{ seats }`, redirect to `data.url`, surface `data.error`. Keep the included-features list and all other copy verbatim. The "Contact us" `href="#"` placeholder stays.

Then run the gates below. Fix any type or lint errors within `app/mockup/**` only (do not modify shared files to satisfy the linter).
  </action>
  <verify>
    <automated>test -f app/mockup/_components/pricing.tsx && grep -q "perSeatRate" app/mockup/_components/pricing.tsx && grep -q "/api/checkout" app/mockup/_components/pricing.tsx && ! grep -q "PRICE_PER_SEAT = 49" app/mockup/_components/pricing.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>pricing.tsx renders a live per-seat rate from the $35/$32/$28 volume bands and a live total, caps seats at 500, and POSTs { seats } to /api/checkout redirecting to data.url. `npx tsc --noEmit` and `npm run lint` both pass clean.</done>
</task>

</tasks>

<verification>
- `/mockup` renders the full v0 concept (header, hero, trust bar, 6 curriculum cards, why/steps, pricing, footer) in the light palette.
- No dark-mode media query anywhere under `app/mockup/**`; the palette is forced light via wrapper CSS vars + `colorScheme: light`.
- `git status` shows ONLY new files under `app/mockup/`; `app/page.tsx`, `app/layout.tsx`, `app/globals.css`, `app/_components/*`, and `components/ui/*` are unchanged.
- `npx tsc --noEmit` passes; `npm run lint` (eslint 9 flat config) passes.
</verification>

<success_criteria>
- New route `/mockup` exists and is fully self-contained (own palette, own `.reveal` CSS, own component copies).
- Pricing reflects the real locked volume bands and drives the existing `/api/checkout` (stepper capped at 500).
- Zero edits to the Athena homepage or any shared/read-only files.
- Both gates (tsc, lint) green.
</success_criteria>

<output>
Create `.planning/quick/260703-g7x-mock-up-v0-homepage-concept-as-mockup-ro/260703-g7x-SUMMARY.md` when done.
</output>
